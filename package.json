{
  "name": "taxrescrm-static-output",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node -e \"console.log('Static gh-pages output already built; no rebuild required.')\""
  }
}
