import { useState, useEffect } from 'react'
import { useSearchParams } from 'react-router-dom'
import { supabase } from '../lib/supabase'
import { etLabelInZone, visitorZone, zoneShort } from '../lib/timezones'
import { resolveProductConfig } from '../lib/productBookingConfig'

const C = { bg:'#0f172a',card:'#1e293b',line:'#334155',text:'#f1f5f9',dim:'#94a3b8',accent:'#2563eb',ok:'#22c55e' }
const fmt12=(t)=>{const[h,m]=t.split(':').map(Number);const ap=h>=12?'PM':'AM';return `${((h+11)%12)+1}:${String(m).padStart(2,'0')} ${ap}`}
export default function BookAppointment(){
 const[params]=useSearchParams(),[cfg,setCfg]=useState(null),[loadErr,setLoadErr]=useState(''),[type,setType]=useState(''),[date,setDate]=useState(''),[slots,setSlots]=useState(null),[time,setTime]=useState(''),[form,setForm]=useState({name:params.get('name')||'',email:params.get('email')||'',phone:params.get('phone')||'',notes:''}),[saving,setSaving]=useState(false),[err,setErr]=useState(''),[done,setDone]=useState(null),[payReturn,setPayReturn]=useState(params.get('booking')||'');const zone=visitorZone()
 const productKey=(params.get('product')||'').toLowerCase().trim(),productCfg=resolveProductConfig(productKey),tid=(params.get('t')||new URLSearchParams(window.location.search).get('t')||'').trim(),TAXRESCRM_TENANT='a0000000-0000-0000-0000-000000000001',tenantArgs={p_tenant:tid||TAXRESCRM_TENANT}
 const[meta,setMeta]=useState({firm_name:productCfg.name,logo_url:productCfg.logo,logo_alt:productCfg.logoAlt,subtitle:productCfg.headline,payment:{required:false}})
 useEffect(()=>{document.title=productCfg.key==='taxres_crm'?'TaxRes CRM — Schedule an Appointment':`${productCfg.name} — ${productCfg.headline}`;const favicon=document.getElementById('app-favicon')||document.querySelector('link[rel="icon"]');const apple=document.getElementById('app-apple-touch-icon')||document.querySelector('link[rel="apple-touch-icon"]');if(productCfg.favicon){if(favicon){favicon.href=productCfg.favicon;favicon.type=productCfg.favicon.includes('.svg')?'image/svg+xml':'image/png'}if(apple)apple.href=productCfg.favicon}},[productCfg.key,productCfg.name,productCfg.headline,productCfg.favicon])
 useEffect(()=>{;(async()=>{const{data,error}=await supabase.rpc('booking_get_config',tenantArgs);if(error||!data){setLoadErr('Online booking is unavailable right now. Please contact us instead.');return}const effectiveTypes=tid?(data.types||[]):productCfg.types;setCfg({...data,types:effectiveTypes});if(effectiveTypes.length)setType(effectiveTypes[0])})();if(tid)supabase.rpc('booking_get_public_meta',{p_tenant:tid}).then(({data})=>{if(data)setMeta(m=>({...m,...data,payment:{...m.payment,...(data.payment||{})}}))}).catch(()=>{})},[])
 const days=[];if(cfg){const keys=['sun','mon','tue','wed','thu','fri','sat'],now=new Date();for(let i=0;i<(cfg.maxDaysOut||30);i++){const d=new Date(now);d.setDate(now.getDate()+i);const key=keys[d.getDay()],blocked=(cfg.blockedDates||[]).includes(d.toISOString().slice(0,10));if(!blocked&&cfg.hours&&cfg.hours[key])days.push({iso:d.toISOString().slice(0,10),label:d.toLocaleDateString('en-US',{weekday:'short',month:'short',day:'numeric'})});if(days.length>=21)break}}
 async function pickDate(iso){setDate(iso);setTime('');setSlots(null);const{data,error}=await supabase.rpc('booking_get_slots',{p_date:iso,...tenantArgs});setSlots(error?[]:(data||[]))}
 async function book(){if(!form.name.trim()||!form.email.trim()){setErr('Please enter your name and email so we can send your confirmation.');return}if(meta.payment?.required){setSaving(true);setErr('');const base=window.location.origin+window.location.pathname;const{data,error}=await supabase.functions.invoke('booking-checkout',{body:{name:form.name.trim(),email:form.email.trim(),phone:form.phone.trim(),event_type:type,date,time,notes:form.notes.trim(),tenant:tid||'',success_url:`${base}?booking=paid`,cancel_url:`${base}?booking=canceled`}});if(error||!data?.url){setSaving(false);setErr((data&&data.error)||'Could not start payment. Please contact us instead.');return}window.location.href=data.url;return}setSaving(true);setErr('');const{data,error}=await supabase.rpc('booking_create_product',{p_name:form.name.trim(),p_email:form.email.trim(),p_phone:form.phone.trim(),p_event_type:type,p_date:date,p_time:time,p_notes:form.notes.trim(),p_product:productCfg.key,...tenantArgs});setSaving(false);if(error){setErr(error.message);return}if(data&&data.ok===false){setErr(data.message||'That time was just taken — please pick another slot.');pickDate(date);return}const token=data&&data.booking_token;if(token){Promise.allSettled([supabase.functions.invoke('send-email',{body:{kind:'booking_confirmation',booking_token:token}}),supabase.functions.invoke('send-email',{body:{kind:'booking_firm_notification',booking_token:token}})]).catch(()=>{})}setDone({date,time,type})}
 const box={background:C.card,border:`1px solid ${C.line}`,borderRadius:12,padding:20},input={width:'100%',boxSizing:'border-box',background:C.bg,border:`1px solid ${C.line}`,borderRadius:8,color:C.text,padding:'10px 12px',fontSize:14},chip=(active)=>({padding:'9px 12px',borderRadius:8,cursor:'pointer',fontSize:13,textAlign:'center',border:`1px solid ${active?C.accent:C.line}`,background:active?C.accent:'transparent',color:C.text});const displayName=meta.firm_name||productCfg.name,displayLogo=meta.logo_url||productCfg.logo,displayLogoAlt=meta.logo_alt||productCfg.logoAlt||displayName,displaySubtitle=meta.subtitle||productCfg.headline
 return <div style={{minHeight:'100vh',background:C.bg,color:C.text,fontFamily:'system-ui, -apple-system, sans-serif',padding:'32px 16px'}}><div style={{maxWidth:640,margin:'0 auto'}}><div style={{textAlign:'center',marginBottom:24}}>{displayLogo&&<img src={displayLogo} alt={displayLogoAlt} style={{maxHeight:56,maxWidth:220,objectFit:'contain',marginBottom:10}} onError={e=>{e.target.style.display='none'}}/>}<div style={{fontSize:24,fontWeight:800}}>{displayName}</div>{displaySubtitle&&<div style={{fontSize:14,color:'#a0b0c8',marginTop:4}}>{displaySubtitle}</div>}</div>{payReturn==='paid'?<div style={{...box,textAlign:'center'}}><div style={{fontSize:40}}>✅</div><div style={{fontWeight:800,fontSize:18,marginTop:8}}>Payment received — you're booked!</div><div style={{color:C.dim,marginTop:8,fontSize:14}}>A confirmation email is on its way.</div></div>:payReturn==='canceled'?<div style={{...box,textAlign:'center'}}><div style={{fontSize:40}}>↩️</div><div style={{fontWeight:800,fontSize:18,marginTop:8}}>Payment canceled</div><button style={{marginTop:16,background:C.accent,color:'#fff',border:'none',borderRadius:8,padding:'10px 20px'}} onClick={()=>setPayReturn('')}>Try again</button></div>:loadErr?<div style={{...box,textAlign:'center',color:C.dim}}>{loadErr}</div>:!cfg?<div style={{...box,textAlign:'center',color:C.dim}}>Loading…</div>:done?<div style={{...box,textAlign:'center'}}><div style={{fontSize:40}}>✅</div><div style={{fontWeight:800,fontSize:18,marginTop:8}}>You're booked!</div><div style={{color:C.dim,marginTop:8,fontSize:14}}>{done.type}<br/>{new Date(done.date+'T12:00:00').toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric'})} at {fmt12(done.time)} (Eastern){zone!=='America/New_York'&&<> — {etLabelInZone(done.date,done.time,zone)} your time</>}</div></div>:<div style={box}>{cfg.types.length>1&&<><div style={{fontWeight:700,fontSize:13.5,marginBottom:8}}>What is this about?</div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill, minmax(180px, 1fr))',gap:8,marginBottom:18}}>{cfg.types.map(t=><div key={t} style={chip(type===t)} onClick={()=>setType(t)}>{t}</div>)}</div></>}<div style={{fontWeight:700,fontSize:13.5,marginBottom:8}}>Pick a day</div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill, minmax(120px, 1fr))',gap:8,marginBottom:18}}>{days.map(d=><div key={d.iso} style={chip(date===d.iso)} onClick={()=>pickDate(d.iso)}>{d.label}</div>)}</div>{date&&<><div style={{fontWeight:700,fontSize:13.5,marginBottom:8}}>Pick a time <span style={{color:C.dim,fontWeight:400}}>({zone==='America/New_York'?'Eastern':`your time — ${zoneShort(zone)}`})</span></div>{slots===null?<div style={{color:C.dim,fontSize:13,marginBottom:18}}>Checking availability…</div>:slots.length===0?<div style={{color:C.dim,fontSize:13,marginBottom:18}}>No open times that day — try another date.</div>:<div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill, minmax(90px, 1fr))',gap:8,marginBottom:18}}>{slots.map(s=><div key={s} style={chip(time===s)} onClick={()=>setTime(s)}>{etLabelInZone(date,s,zone)}</div>)}</div>}</>}{time&&<><div style={{fontWeight:700,fontSize:13.5,marginBottom:8}}>Your details</div><div style={{display:'grid',gap:10,marginBottom:14}}><input style={input} placeholder="Full name *" value={form.name} onChange={e=>setForm(f=>({...f,name:e.target.value}))}/><input style={input} placeholder="Email *" type="email" value={form.email} onChange={e=>setForm(f=>({...f,email:e.target.value}))}/><input style={input} placeholder="Phone" value={form.phone} onChange={e=>setForm(f=>({...f,phone:e.target.value}))}/><textarea style={{...input,resize:'vertical'}} rows={2} placeholder="Anything we should know? (optional)" value={form.notes} onChange={e=>setForm(f=>({...f,notes:e.target.value}))}/></div>{err&&<div style={{color:'#f87171',fontSize:13,marginBottom:10}}>{err}</div>}<button disabled={saving} style={{width:'100%',background:C.accent,color:'#fff',border:'none',borderRadius:8,padding:'12px 0',fontSize:15,fontWeight:700,cursor:'pointer',opacity:saving?.7:1}} onClick={book}>{saving?'Booking…':`Confirm — ${etLabelInZone(date,time,zone)} on ${new Date(date+'T12:00:00').toLocaleDateString('en-US',{month:'short',day:'numeric'})}`}</button></>}</div>}<div style={{textAlign:'center',color:C.dim,fontSize:11.5,marginTop:16}}>{displayName}</div></div></div>
}
