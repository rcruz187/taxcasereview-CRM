name: RomyLabs Meet Admin Hub Patch

on:
  push:
    branches:
      - feature/romylabs-meet-admin-hub
    paths:
      - '.github/workflows/romylabs-meet-admin-hub.yml'
      - 'scripts/romylabs-meet-admin-patch.mjs'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/romylabs-meet-admin-hub
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply targeted Admin Portal patch
        run: node scripts/romylabs-meet-admin-patch.mjs
      - name: Commit patch if needed
        run: |
          if git diff --quiet; then
            echo "No changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/AdminPortal.jsx
          git commit -m "feat: expose RomyLabs Meet in admin portal"
          git push origin HEAD:feature/romylabs-meet-admin-hub
