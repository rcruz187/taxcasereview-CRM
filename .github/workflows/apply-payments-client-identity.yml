name: Apply Payments Client Identity Fix

on:
  push:
    paths:
      - '.github/scripts/fix_payments_client_identity.py'
      - '.github/workflows/apply-payments-client-identity.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Payments stable identity fix
        run: python .github/scripts/fix_payments_client_identity.py
      - name: Commit repaired source
        run: |
          if git diff --quiet; then
            echo 'No source changes required.'
            exit 0
          fi
          git config user.name 'RomyLabs Audit Bot'
          git config user.email 'actions@github.com'
          git add src/pages/Payments.jsx
          git commit -m 'fix(payments): preserve stable client identity'
          git push origin HEAD:main
